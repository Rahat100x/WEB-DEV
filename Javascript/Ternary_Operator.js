let age = 18;
// let type = "";
// if(age >= 10) {
//     type = "adult";
// } else {
//     type = "Child";
// }

//ternary
let type = (age >= 10) ? "adult" : age < 10 ? "Child" : "Young";

consle.log(type);o